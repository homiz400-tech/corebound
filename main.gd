extends Node2D

# Minimal host scene for the control prototype. Just shows the drill moving
# through a static "layer band" so the one-thumb dig feel can be tested.
# No game logic here yet — that lives in drill.gd.

func _ready() -> void:
	pass
